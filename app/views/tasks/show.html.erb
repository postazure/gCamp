<p>
  <strong>Description:</strong>
  <%= @task.description %>
</p>
<p>
  <strong>Completed:</strong>
  <%= @task.completed %>
</p>
<p>
  <strong>Due Date:</strong>
  <%= @task.due_date.strftime("%m/%d/%Y") unless @task.due_date.nil? %>
</p>




<%= link_to 'Edit', edit_project_task_path(params[:id],@task) %> |
<%= link_to 'Back', project_tasks_path %>
